# 开发环境覆盖配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/agent_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672

pgvector:
  url: jdbc:postgresql://localhost:5432/agent_vector
  username: postgres
  password: 123456

logging:
  level:
    com.agent: debug
    root: info
